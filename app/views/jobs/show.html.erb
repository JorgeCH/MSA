<div class="main" id="two">
  <div class="container">
    <div class="content">
     	<div class="left-cont">
		    <h3><p>Contacto: <%= link_to @job.contact, @job.contact %></p></h3>
		    </p></h3>
		    <h3>
		    	<% if current_user %> 
					<p><%= link_to 'Edit', edit_job_path(@job) %></p>
		        <% end %>
		    </h3>
		    <div class="pad"><a href="/jobs"><button>Back</button></a>
		    </div>
    	</div>
      	<div class="right-cont">
            <hr class="first">
            <h1><%= @job.title %></h1>
            <hr class="second">
            <p><%= simple_format(@job.description) %></p>
      	</div>
      </div>  
    </div>
</div>