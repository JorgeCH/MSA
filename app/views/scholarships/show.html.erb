<div class="main" id="two">
  <div class="container">
    <div class="content">
     	<div class="left-cont">
		    <h3><p>Dirección: <%= link_to(@scholarship.address) %></p></h3>
		    <h3><p>Link: <%= link_to @scholarship.link, @scholarship.link%></p></h3>
		    <h3>
		    	<% if current_user %> 
					<p><%= link_to 'Edit', edit_scholarship_path(@scholarship) %></p>
		        <% end %>
		    </h3>
		    <div class="pad"><a href="/scholarships"><button>Back</button></a>
		    </div>
    	</div>
      	<div class="right-cont">
            <hr class="first">
            <h1><%= @scholarship.name %></h1>
            <hr class="second">
            <p><%= simple_format(@scholarship.description) %></p>
          </div>
      	</div>
    </div>
</div>